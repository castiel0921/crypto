# crypto

`crypto` 是当前工作目录下初始化的项目仓库。

## 当前状态

- 已准备为 GitHub 仓库使用
- 默认分支将使用 `main`
- 远程仓库将配置为 `git@github.com:castiel0921/crypto.git`

## 后续使用

初始化完成后，可以直接在这个目录继续开发并推送：

```bash
git add .
git commit -m "your message"
git push -u origin main
```
